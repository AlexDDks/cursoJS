// 1. Operadores de asignación

// El operador de asignación =, que simplemente asigna el valor de la derecha al operando de la izquierda.
// let x = 5; // asigna 5 a x

// Hay también operadores de asignación compuesta que realizan una operación y asignan el resultado, como +=, -=, *=, /=, etc.

// x += 5; // es equivalente a x = x + 5;
// x -= 5; // x = x - 5;
// x *= 5; //x = x * 5;

// 2. Operadores aritméticos
// Estos operadores se utilizan para realizar operaciones matemáticas comunes:

// Suma: +
// Resta: -
// Multiplicación: *
// División: /
// Módulo (resto de la división):
// Incremento (suma uno al operando): ++
// Decremento (resta uno al operando): --
// Exponenciación (potencia): **                        


// 3. Operadores de Comparación
// Los operadores de comparación comparan dos operandos y devuelven un valor booleano, true o false.

// Igual (==)
// No igual (!=)
// Estrictamente igual (===)
// Estrictamente no igual (!==)
// Mayor que (>)
// Menor que (<)
// Mayor o igual que (>=)
// Menor o igual que (<=) 

// 4. Operadores Lógicos
// Se utilizan para determinar la lógica entre variables o valores:

// AND lógico (&&)
// OR lógico (||)
// NOT lógico (!)

// // 5. Operadores de String
// Concatenación: +
// "hola "+" mi nombres es :"
// // Concatenación con asignación: +=

let x = 1;
// AQUI X VALE 1

x += 5;

X = 1 + 5;
// AQUÍ X VALE 6

x = y; //ASIGNANDO EL VALOR DE Y A X;
x == y; // COMPARANDO VALOR
